from setuptools import setup, find_packages
import sys, os

version = '0.1'

setup(name='taylan',
      version=version,
      description="Repair status of escalators in the NYC subway system",
      long_description="""\
Repair status of escalators in the NYC subway system (2nd assignment)""",
      classifiers=[], # Get strings from http://pypi.python.org/pypi?%3Aaction=list_classifiers
      keywords='repair',
      author='Taylan',
      author_email='fulya.taylan@scilifelab.se',
      url='',
      license='',
      scripts = ['script/getting_data.py'],
      packages=find_packages(exclude=['ez_setup', 'examples', 'tests']),
      include_package_data=True,
      zip_safe=False,
      install_requires=[
          # -*- Extra requirements: -*-
      ],
      entry_points="""
      # -*- Entry points: -*-
      """,
      )
